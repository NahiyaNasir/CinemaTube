enum Role {
  ADMIN
  USER
}

enum UserStatus {
  BLOCKED
  DELETED
  ACTIVE
  PENDING
  UNVERIFIED
}

enum MediaType {
  MOVIE
  SERIES
}

enum ReviewStatus {
  PENDING
  APPROVED
  REJECTED
}

enum PaymentStatus {
  PAID
  UNPAID
}

enum MediaPurchaseType {
  RENTAL
  BUY
}

enum MediaPurchaseStatus {
  ACTIVE
  EXPIRED
  CANCELLED
}

enum RentalStatus {
  ACTIVE
  EXPIRED
  CANCELLED
}

enum PurchaseType {
  BUY
  RENT
}

enum Pricing {
  FREE
  PREMIUM
  RENTAL
}

enum SubscriptionPlan {
  FREE
  MONTHLY
  YEARLY
}

enum SubscriptionStatus {
  ACTIVE
  CANCELLED
  EXPIRED
  PAST_DUE
}

enum SubStatus {
  ACTIVE
  CANCELLED
  EXPIRED
}
